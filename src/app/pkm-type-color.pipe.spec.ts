import { PkmTypeColorPipe } from './pkm-type-color.pipe';

describe('PkmTypeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new PkmTypeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
